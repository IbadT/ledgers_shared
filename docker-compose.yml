services:
  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:-guest}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASS:-guest}
      RABBITMQ_DEFAULT_VHOST: /
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq/
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]  # ← исправлено
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - microservices-network
    restart: unless-stopped

volumes:
  rabbitmq_data:

networks:
  microservices-network:
    driver: bridge






# services:
#   rabbitmq:
#     image: rabbitmq:3-management-alpine
#     container_name: rabbitmq
#     ports:
#       - "5672:5672"
#       - "15672:15672"
#     environment:
#       RABBITMQ_HOST: ${RABBITMQ_HOST}
#       RABBITMQ_PORT: ${RABBITMQ_PORT}
#       RABBITMQ_USER: ${RABBITMQ_USER}
#       RABBITMQ_PASS: ${RABBITMQ_PASS}
#       RABBITMQ_URI: ${RABBITMQ_URI}
#     volumes:
#       - rabbitmq_data:/var/lib/rabbitmq/
#     healthcheck:
#       test: ["CMD", "rabbitmq-deagnotics", "-q", "ping"]
#       interval: 30s
#       timeout: 10s
#       retries: 3
#     networks:
#       - microservices-network
#     restart: unless-stopped

# volumes:
#   rabbitmq_data:

# networks:
#   microservices-network:
#     driver: bridge